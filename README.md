# Compact Cookie Clicker Package

Required code files:
- index.html
- style.css
- app.js

`app.js` bundles the Base64 helper, main game engine, Garden, Stock Market, Pantheon, and Grimoire minigames.

The original game still references `img/` and `snd/` asset folders, which were not included in the uploaded files.
